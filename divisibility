#include <stdio.h>
int main() {
    int n ;
    printf("enter a number:");
    scanf("%d",&n);
    if(n%3==0)
    {
        printf("fuzz");
    }
    if(n%5==0)
    {
        printf("buzz");
        
    }
    if(n%15==0)
    {
        printf("fuzzbuzz");
    }
    return 0;
}
