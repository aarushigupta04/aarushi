#include<stdio.h>
int main()
 {
    float r,a,p;
    float pi=3.14;
    printf("radius of circle:");
    scanf("%f",&r);
    a=pi*r*r;
    p=2*pi*r;
    printf("area of circle:%f",a);
    printf("\nperimeter of circle:%f",p);
    return 0;
 }
